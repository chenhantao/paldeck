# Changelog

本项目的重要变更将记录在此文件中。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循
[Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### Added

- 首页停止和重启操作增加安全确认流程：可自定义广播与 0–300 秒倒计时，广播完成后
  调用 REST 保存并验证存档，只有保存成功才执行 Compose 操作。
- 按平台拆分桌面架构：Tauri 自动合并 Windows、macOS、Linux 原生配置，前端统一
  平台检测并分别加载平台样式，共享布局通过 CSS 变量读取平台 token。

### Fixed

- 提升 Windows、macOS 与 Linux 的共享字号和控件可读性；Windows 额外优先使用
  Segoe UI 与微软雅黑 UI 字体，改善小字号文字发虚的问题。
- Windows 主内容区不再受固定 1180 像素宽度限制；页面会随窗口流式扩展，并在
  1440 像素以上同步调整侧栏、标题、概览卡片和双栏面板。其他平台布局保持不变。
- 首页普通重启不再强制重新创建容器；只有应用世界配置或备份策略时才使用
  `up -d --force-recreate` 载入新的环境变量。

## [0.1.0] - 2026-08-17

### Added

- Palworld Docker Compose 部署配置与环境变量模板。
- Paldeck Tauri 桌面客户端 Preview。
- 带输入验证、超时和操作白名单的 SSH 后端基础能力。
- 带接管/新建流程的首次使用初始化向导。
- OpenSSH 配置/密钥和直接账号密码两种登录方式。
- 账号密码模式的 SSH 主机密钥指纹确认与固定。
- 多服务器配置管理，可新增、切换、编辑和仅在本地删除连接；旧版单服务器记录会
  自动迁移，删除最后一条记录后返回初始化向导。该功能在首版中标记为 Beta。
- 默认使用远程账号的 `~/.palworld` 作为部署目录。
- 对 Linux、amd64、Docker、Compose 和现有部署有效性的远程检查。
- 开源项目治理文件和持续集成配置。
- 从 `master` 语义化版本标签构建三平台资产、生成校验和并创建 GitHub Release
  草稿的正式发布工作流。
- 支持跟随系统、简体中文和 English 的桌面界面，并在本地保存语言偏好。
- 总览、在线玩家、日志、备份列表和世界配置页面接入真实远程数据与操作。
- 世界配置编辑器按 8 个分组覆盖固定容器版本支持的 79 项白名单参数；保存旧部署
  时会校验、备份并升级 Paldeck Compose 模板。
- 自动备份策略、备份删除和事务式恢复以 Beta 状态随首版提供，并在界面及文档中
  提醒用户保留额外存档。

### Fixed

- 切换服务器时立即清空上一台服务器的状态，并忽略切换前尚未完成的状态请求，避免
  旧服务器数据覆盖当前界面。
- 保存世界现在检查 REST 调用结果，并在已有存档的情况下等待和验证 `Level.sav`
  确实更新；点击后立即显示保存进度，保留 `rest-cli` 的真实失败退出码，成功反馈
  会区分“API 已接受”和“存档文件已确认更新”。
- “重启”改为重新创建容器，使修改后的 `.env` 真正进入容器；原来的普通
  `docker compose restart` 不会重新读取环境变量。
- 世界配置保存成功后提供“立即应用”和“稍后应用”；立即应用会重新创建容器并刷新
  服务器状态，稍后应用则保留已验证的 `.env` 等待下次重启。
- 备份页面现在可以编辑自动备份开关、五段式 Cron 计划、旧备份清理开关和保留
  天数，并在保存后选择立即重建容器应用或稍后应用。
- 支持从界面删除指定归档；文件名、数据根目录、普通文件和符号链接边界均在 Rust
  后端再次校验。
- 支持事务式恢复指定备份：恢复前校验归档并停止服务器，保留当前世界的恢复前安全
  归档，失败时回滚，并将服务器恢复到操作前的运行或停止状态。
- 手动备份超时延长到 15 分钟，并在成功提示前确认备份目录中确实出现了新归档。
- 首页和玩家管理页每 10 秒刷新在线玩家，并在应用重新获得焦点时立即刷新；临时
  请求失败时保留最近一次成功获取的玩家列表。
- 用应用内广播对话框替代桌面 WebView 中不可靠的浏览器提示框，并接入真实的
  Palworld REST 广播操作与发送状态反馈。
- 修复前后端 OpenSSH 参数命名不一致导致的连接参数解析失败；OpenSSH 模式现在
  分别要求用户名和 Host，并明确以 `用户名@Host` 调用系统 SSH 客户端。
- 修复容器镜像标签缺少 `v` 导致无法拉取的问题，并默认在初始化后启动服务器。
- Compose 控制现在显示执行中状态、阻止重复操作、反馈结果并刷新真实容器状态。
- 删除玩家、性能、日志和备份演示数据；无法获取的值明确显示为不可用。
- Windows 发布版不再额外显示控制台窗口。
- 桌面界面可随 Windows、macOS 和 Linux 窗口宽度响应式调整。
- 使用管理标记区分 Paldeck 部署，拒绝修改非空的未受管同名目录。
- 收紧远程路径、符号链接、临时文件及 Compose 数据目录边界。
- 支持在部署目录内自定义 Palworld 数据子目录，并在每次管理操作前检查路径和
  Compose 模板摘要。
- macOS 开发包现在使用完整的 ad-hoc 签名，并在上传前执行严格签名校验，避免
  GitHub 下载产物因不完整签名被报告为已损坏。
- Unix 桌面构建静态编译 OpenSSL，避免产物依赖构建机上的 Homebrew 或系统
  OpenSSL 路径。
- 为旧版 WebKit 提供 UUID 回退，并在本地存储不可用时保持语言选择功能可启动。
- 三平台开发包增加自动关闭的启动存活测试；macOS 额外检查动态库边界与压缩包
  解压后的签名。
