# 安全策略

## 支持范围

Paldeck 仍处于 `0.x` 早期开发阶段，目前只对最新的 `master` 分支提供安全修复。

## 报告漏洞

请使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告功能。不要为
安全漏洞创建公开 Issue，也不要在报告中附带真实密码、SSH 私钥或服务器存档。

报告中建议包含：

- 受影响的版本或提交
- 可复现的最小步骤
- 可能造成的影响
- 已知的缓解方式

尤其欢迎报告命令注入、路径绕过、凭据泄漏、权限提升和不安全默认配置。维护者会
尽快确认报告；在修复可用前，请避免公开披露复现细节。
